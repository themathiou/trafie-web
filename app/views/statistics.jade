div#main_content( ng-init="statisticsInit( user._id, user.discipline)" )
	br
	br
	h4= user.first_name
	
	ul.filters-row
		li( ng-repeat="(key, value) in user.disciplines_of_user")
			a( ng-click="drawSimpleChart( user._id, key )" ) {{value}}

	div#loading.graphs_loading( ng-show="loading")
		p Your awesome history is loading! Please wait...

	div#dashboard
		div#chart_div.chart-div
		div#control( style='width: 100%; height: 50px;' )


